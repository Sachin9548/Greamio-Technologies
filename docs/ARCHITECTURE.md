# 🏗️ CloudBlitz CRM - System Architecture

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USERS / CLIENTS                            │
│  👤 Admin    👤 Staff    👤 Regular Users    👤 Customers           │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ HTTPS
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      PRESENTATION LAYER                             │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                    FRONTEND (Vercel)                          │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  React 18 Application                                   │ │ │
│  │  │  ├── Welcome Page (Landing)                             │ │ │
│  │  │  ├── Authentication (Login/Register)                    │ │ │
│  │  │  ├── Enquiry Dashboard (Main)                           │ │ │
│  │  │  ├── User Management (Admin)                            │ │ │
│  │  │  └── Modals (Add/Edit/View)                             │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  UI Components                                          │ │ │
│  │  │  ├── Tailwind CSS (Styling)                             │ │ │
│  │  │  ├── React Router (Navigation)                          │ │ │
│  │  │  ├── React Hot Toast (Notifications)                    │ │ │
│  │  │  └── Context API (State Management)                     │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  Build & Deployment                                     │ │ │
│  │  │  ├── Vite (Build Tool)                                  │ │ │
│  │  │  ├── Nginx (Production Server)                          │ │ │
│  │  │  └── Vercel CDN (Global Distribution)                   │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ REST API (JSON)
                                  │ JWT Token Authentication
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      APPLICATION LAYER                              │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                    BACKEND (Render.com)                       │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  API Routes                                             │ │ │
│  │  │  ├── /api/auth (Authentication)                         │ │ │
│  │  │  │   ├── POST /register                                 │ │ │
│  │  │  │   ├── POST /login                                    │ │ │
│  │  │  │   └── GET /me                                        │ │ │
│  │  │  ├── /api/enquiries (Enquiry Management)               │ │ │
│  │  │  │   ├── POST / (Create)                                │ │ │
│  │  │  │   ├── GET / (List with filters)                      │ │ │
│  │  │  │   ├── GET /:id (Get single)                          │ │ │
│  │  │  │   ├── PUT /:id (Update)                              │ │ │
│  │  │  │   └── DELETE /:id (Soft delete)                      │ │ │
│  │  │  ├── /api/users (User Management - Admin)              │ │ │
│  │  │  │   ├── GET / (List all)                               │ │ │
│  │  │  │   ├── POST / (Create)                                │ │ │
│  │  │  │   ├── PUT /:id (Update)                              │ │ │
│  │  │  │   └── DELETE /:id (Delete)                           │ │ │
│  │  │  └── /api/health (Health Check)                         │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  Middleware Layer                                       │ │ │
│  │  │  ├── CORS (Cross-Origin)                                │ │ │
│  │  │  ├── Body Parser (JSON)                                 │ │ │
│  │  │  ├── Authentication (JWT Verify)                        │ │ │
│  │  │  ├── Authorization (Role Check)                         │ │ │
│  │  │  ├── Validation (Zod)                                   │ │ │
│  │  │  └── Error Handler (Global)                             │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  Business Logic (Controllers)                           │ │ │
│  │  │  ├── auth.controller.js                                 │ │ │
│  │  │  │   ├── Register user                                  │ │ │
│  │  │  │   ├── Login user                                     │ │ │
│  │  │  │   └── Get current user                               │ │ │
│  │  │  ├── enquiry.controller.js                              │ │ │
│  │  │  │   ├── Create enquiry                                 │ │ │
│  │  │  │   ├── Get enquiries (with filters)                   │ │ │
│  │  │  │   ├── Update enquiry                                 │ │ │
│  │  │  │   └── Delete enquiry                                 │ │ │
│  │  │  └── user.controller.js                                 │ │ │
│  │  │      ├── Get all users                                  │ │ │
│  │  │      ├── Create user                                    │ │ │
│  │  │      ├── Update user                                    │ │ │
│  │  │      └── Delete user                                    │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  Security Layer                                         │ │ │
│  │  │  ├── JWT (Token Generation & Verification)             │ │ │
│  │  │  ├── Bcrypt (Password Hashing)                          │ │ │
│  │  │  ├── Zod (Input Validation)                             │ │ │
│  │  │  └── Role-Based Access Control                          │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Mongoose ODM
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                                   │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                MongoDB Atlas (Cloud Database)                 │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  Collections                                            │ │ │
│  │  │  ┌───────────────────────────────────────────────────┐ │ │ │
│  │  │  │  users                                            │ │ │ │
│  │  │  │  ├── _id (ObjectId)                               │ │ │ │
│  │  │  │  ├── name (String)                                │ │ │ │
│  │  │  │  ├── email (String, unique)                       │ │ │ │
│  │  │  │  ├── passwordHash (String)                        │ │ │ │
│  │  │  │  ├── role (String: admin/staff/user)             │ │ │ │
│  │  │  │  ├── createdAt (Date)                             │ │ │ │
│  │  │  │  └── updatedAt (Date)                             │ │ │ │
│  │  │  └───────────────────────────────────────────────────┘ │ │ │
│  │  │  ┌───────────────────────────────────────────────────┐ │ │ │
│  │  │  │  enquiries                                        │ │ │ │
│  │  │  │  ├── _id (ObjectId)                               │ │ │ │
│  │  │  │  ├── customerName (String)                        │ │ │ │
│  │  │  │  ├── email (String)                               │ │ │ │
│  │  │  │  ├── phone (String)                               │ │ │ │
│  │  │  │  ├── message (String)                             │ │ │ │
│  │  │  │  ├── status (String: new/in-progress/closed)     │ │ │ │
│  │  │  │  ├── assignedTo (ObjectId → users)               │ │ │ │
│  │  │  │  ├── createdBy (ObjectId → users)                │ │ │ │
│  │  │  │  ├── isDeleted (Boolean)                          │ │ │ │
│  │  │  │  ├── createdAt (Date)                             │ │ │ │
│  │  │  │  └── updatedAt (Date)                             │ │ │ │
│  │  │  └───────────────────────────────────────────────────┘ │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  Indexes (Performance)                                  │ │ │
│  │  │  ├── users.email (unique)                               │ │ │
│  │  │  ├── users.role                                         │ │ │
│  │  │  ├── enquiries.status + isDeleted                       │ │ │
│  │  │  ├── enquiries.createdBy + isDeleted                    │ │ │
│  │  │  ├── enquiries.assignedTo                               │ │ │
│  │  │  ├── enquiries.createdAt (desc)                         │ │ │
│  │  │  └── enquiries (text search: name, email, phone)        │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### User Authentication Flow
```
┌──────┐     ┌──────────┐     ┌─────────┐     ┌──────────┐
│ User │────▶│ Frontend │────▶│ Backend │────▶│ MongoDB  │
└──────┘     └──────────┘     └─────────┘     └──────────┘
   │              │                 │                │
   │ 1. Enter     │ 2. POST         │ 3. Hash        │
   │ credentials  │ /auth/login     │ password       │
   │              │                 │                │
   │              │                 │ 4. Query       │
   │              │                 │ user ─────────▶│
   │              │                 │                │
   │              │                 │ 5. Return      │
   │              │                 │ user ◀─────────│
   │              │                 │                │
   │              │                 │ 6. Compare     │
   │              │                 │ passwords      │
   │              │                 │                │
   │              │                 │ 7. Generate    │
   │              │                 │ JWT token      │
   │              │                 │                │
   │              │ 8. Return       │                │
   │              │ token + user ◀──│                │
   │              │                 │                │
   │ 9. Store     │                 │                │
   │ token ◀──────│                 │                │
   │              │                 │                │
   │ 10. Redirect │                 │                │
   │ to dashboard │                 │                │
   └──────────────┘                 │                │
```

### Enquiry Creation Flow
```
┌──────┐     ┌──────────┐     ┌─────────┐     ┌──────────┐
│ User │────▶│ Frontend │────▶│ Backend │────▶│ MongoDB  │
└──────┘     └──────────┘     └─────────┘     └──────────┘
   │              │                 │                │
   │ 1. Fill      │ 2. POST         │ 3. Verify      │
   │ enquiry form │ /enquiries      │ JWT token      │
   │              │ + token         │                │
   │              │                 │                │
   │              │                 │ 4. Validate    │
   │              │                 │ input (Zod)    │
   │              │                 │                │
   │              │                 │ 5. Check       │
   │              │                 │ permissions    │
   │              │                 │                │
   │              │                 │ 6. Create      │
   │              │                 │ enquiry ───────▶│
   │              │                 │                │
   │              │                 │ 7. Return      │
   │              │                 │ saved data ◀───│
   │              │                 │                │
   │              │ 8. Return       │                │
   │              │ enquiry ◀───────│                │
   │              │                 │                │
   │ 9. Show      │                 │                │
   │ success ◀────│                 │                │
   │ toast        │                 │                │
   │              │                 │                │
   │ 10. Refresh  │                 │                │
   │ list         │                 │                │
   └──────────────┘                 │                │
```

### Permission Check Flow
```
Request → JWT Middleware → Role Middleware → Controller
   │            │                 │               │
   │            │                 │               │
   ▼            ▼                 ▼               ▼
Extract      Verify           Check           Execute
Token        Token            Role            Business
             │                │               Logic
             │                │               │
             ▼                ▼               ▼
         Valid?           Has              Query
         Yes/No        Permission?         Database
             │            Yes/No              │
             │                │               │
             ▼                ▼               ▼
         Continue         Continue         Return
         or 401           or 403          Response
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   SECURITY LAYERS                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Layer 1: Transport Security                           │
│  ├── HTTPS (SSL/TLS)                                   │
│  ├── Secure Headers                                    │
│  └── CORS Configuration                                │
│                                                         │
│  Layer 2: Authentication                               │
│  ├── JWT Token (7-day expiry)                          │
│  ├── Password Hashing (bcrypt, 10 rounds)             │
│  └── Token Verification                                │
│                                                         │
│  Layer 3: Authorization                                │
│  ├── Role-Based Access Control                         │
│  │   ├── Admin (full access)                           │
│  │   ├── Staff (enquiry management)                    │
│  │   └── User (own enquiries only)                     │
│  └── Ownership Verification                            │
│                                                         │
│  Layer 4: Input Validation                             │
│  ├── Zod Schema Validation                             │
│  ├── Email Format Check                                │
│  ├── Password Strength                                 │
│  └── Data Sanitization                                 │
│                                                         │
│  Layer 5: Database Security                            │
│  ├── MongoDB Atlas (encrypted at rest)                │
│  ├── Connection String (environment variable)          │
│  ├── IP Whitelist                                      │
│  └── Soft Delete (data recovery)                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    PRODUCTION ENVIRONMENT                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────┐         ┌─────────────────┐          │
│  │   Vercel CDN    │         │  Render.com     │          │
│  │   (Frontend)    │◀───────▶│  (Backend)      │          │
│  │                 │   API   │                 │          │
│  │  - React Build  │  Calls  │  - Node.js      │          │
│  │  - Nginx Serve  │         │  - Express API  │          │
│  │  - Global CDN   │         │  - Auto-deploy  │          │
│  │  - Auto-deploy  │         │  - Health check │          │
│  └─────────────────┘         └─────────────────┘          │
│         │                             │                    │
│         │                             │                    │
│         │                             ▼                    │
│         │                    ┌─────────────────┐          │
│         │                    │  MongoDB Atlas  │          │
│         │                    │  (Database)     │          │
│         │                    │                 │          │
│         │                    │  - M0 Free Tier │          │
│         │                    │  - Auto-backup  │          │
│         │                    │  - Monitoring   │          │
│         │                    └─────────────────┘          │
│         │                                                  │
│         ▼                                                  │
│  ┌─────────────────┐                                      │
│  │   GitHub Repo   │                                      │
│  │                 │                                      │
│  │  - Source Code  │                                      │
│  │  - CI/CD        │                                      │
│  │  - Auto-deploy  │                                      │
│  └─────────────────┘                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 CI/CD Pipeline

```
┌──────────────────────────────────────────────────────────┐
│                    DEVELOPMENT WORKFLOW                   │
└──────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────┐
│  Developer pushes code to GitHub                         │
└──────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────┐
│  GitHub Actions (CI/CD)                                  │
│  ├── Run tests                                           │
│  ├── Check linting                                       │
│  ├── Build application                                   │
│  └── Deploy if tests pass                                │
└──────────────────────────────────────────────────────────┘
                          │
                ┌─────────┴─────────┐
                │                   │
                ▼                   ▼
┌─────────────────────┐   ┌─────────────────────┐
│  Vercel Deploy      │   │  Render Deploy      │
│  (Frontend)         │   │  (Backend)          │
│  - Build React      │   │  - Install deps     │
│  - Deploy to CDN    │   │  - Start server     │
│  - Update DNS       │   │  - Health check     │
└─────────────────────┘   └─────────────────────┘
                │                   │
                └─────────┬─────────┘
                          ▼
┌──────────────────────────────────────────────────────────┐
│  Application Live                                        │
│  ✅ Frontend: https://your-app.vercel.app               │
│  ✅ Backend: https://your-api.onrender.com              │
└──────────────────────────────────────────────────────────┘
```

---

## 📊 Technology Stack Details

### Frontend Stack
```
React 18.2.0
├── Vite 5.0.0 (Build Tool)
├── Tailwind CSS 3.4.0 (Styling)
├── React Router DOM 6.20.0 (Routing)
├── React Hot Toast 2.4.1 (Notifications)
└── Fetch API (HTTP Client)
```

### Backend Stack
```
Node.js 20.x
├── Express 4.18.2 (Web Framework)
├── Mongoose 8.0.3 (MongoDB ODM)
├── JWT 9.0.2 (Authentication)
├── Bcrypt 2.4.3 (Password Hashing)
├── Zod 3.22.4 (Validation)
└── CORS 2.8.5 (Cross-Origin)
```

### Database
```
MongoDB Atlas
├── Version: 7.0
├── Tier: M0 (Free)
├── Storage: 512MB
└── Backup: Automatic
```

---

## 🎯 Performance Optimizations

### Frontend
- ✅ Code splitting (Vite)
- ✅ Lazy loading components
- ✅ CDN delivery (Vercel)
- ✅ Gzip compression
- ✅ Asset caching
- ✅ Minified builds

### Backend
- ✅ Database indexing
- ✅ Pagination (50 items/page)
- ✅ Query optimization
- ✅ Connection pooling
- ✅ Response caching (headers)

### Database
- ✅ Compound indexes
- ✅ Text search indexes
- ✅ Query optimization
- ✅ Connection pooling

---

## 📈 Scalability

### Current Capacity
- **Users:** 100+ concurrent
- **Enquiries:** 10,000+
- **API Requests:** 1000/min
- **Response Time:** <500ms

### Scaling Options
- **Horizontal:** Add more Render instances
- **Vertical:** Upgrade Render plan
- **Database:** Upgrade MongoDB tier
- **CDN:** Vercel auto-scales

---

**Version:** 1.0.0  
**Last Updated:** January 2025  
**Status:** Production Ready ✅
